[package]
name = "pixbar-device"
edition.workspace = true
version.workspace = true
license.workspace = true
rust-version.workspace = true
readme.workspace = true
repository.workspace = true
publish.workspace = true
description = "The program that runs on the Ulanzi TC002 in place of its stock app"

[dependencies]
libc = "0.2"
pixbar-render = { path = "../pixbar-render" }
pixbar-proto = { path = "../pixbar-proto" }
